interface:
  display_name: "HTML Plan (Legacy)"
  short_description: "Compatibility alias for HTML plans"
  default_prompt: "Use $html-plan to complete this legacy plan request, then recommend $html for future work."

policy:
  allow_implicit_invocation: false
