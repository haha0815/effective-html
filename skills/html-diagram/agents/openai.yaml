interface:
  display_name: "HTML Diagram (Legacy)"
  short_description: "Compatibility alias for HTML diagrams"
  default_prompt: "Use $html-diagram to complete this legacy diagram request, then recommend $html for future work."

policy:
  allow_implicit_invocation: false
